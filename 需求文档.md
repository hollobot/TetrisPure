# TetrisPure 项目需求文档

## 一、项目概述

本项目是一款基于前端技术栈实现的经典俄罗斯方块游戏，全程无后端服务依赖，所有游戏逻辑、渲染计算、数据存储均在浏览器本地完成，可作为纯静态资源直接部署上线。

项目采用「逻辑层与渲染层完全解耦」的架构设计，核心游戏算法独立封装，视图层基于 Canvas 2D 实现逐帧高帧率渲染。完整还原官方标准玩法，同时优化操作手感与视觉反馈，既可作为可交互的趣味作品，也可作为前端渲染能力与算法实现的工程化实践项目，适配个人作品集与博客站点集成。

## 二、技术栈选型

### 2.1 核心基础技术栈

|   分类   |                   技术选型                    |                说明                |
| :------: | :-------------------------------------------: | :--------------------------------: |
| 工程构建 |                   Vite 5.x                    | 负责项目打包、热更新与静态资源构建 |
| 前端框架 | Vue 3.x（Composition API + `<script setup>`） |      页面架构与响应式状态管理      |
| 样式方案 |               Tailwind CSS 3.x                | 实现外围 UI、菜单、面板的样式开发  |
|  图标库  |                lucide-vue-next                |       统一按钮与功能图标风格       |

### 2.2 游戏核心技术

- **渲染引擎**：原生 Canvas 2D API，负责游戏主面板、方块预览区的图形绘制与动效实现
- **主循环机制**：`requestAnimationFrame` 原生 API，构建稳定高帧率游戏循环，精准控制下落速度
- **逻辑封装**：原生 JS 封装为 Vue Composable，实现游戏算法与视图层解耦
- **状态管理**：Vue 原生 `reactive` / `ref`，无需引入额外状态管理库
- **数据持久化**：原生 `localStorage`，本地存储最高分与用户配置

### 2.3 可选增强技术

- 音效管理：`howler.js`，处理操作反馈与消行音效
- 离线能力：`vite-plugin-pwa`，实现桌面安装与离线游玩
- 移动端适配：原生触摸事件 API，支持触控操作

### 2.4 部署方案

纯静态资源部署，支持 GitHub Pages、Vercel、Netlify 等平台，可作为子页面集成至个人站点。

## 三、核心功能需求

### 3.1 游戏核心玩法

1. **方块系统**：支持 7 种标准俄罗斯方块（I/O/T/S/Z/J/L），随机生成，每种方块对应专属配色
2. **旋转机制**：实现矩阵旋转算法，配套墙踢（Wall Kick）逻辑，避免贴墙旋转卡滞
3. **操作控制**：支持键盘左右移动、顺时针旋转、软降（加速下落）、硬降（直接落底）、暂停 / 继续
4. **碰撞检测**：实时判定边界碰撞、堆叠方块碰撞，所有位移与旋转操作前均做合法性校验
5. **消行计分**：满行自动消除，支持多行消除连击倍率计分，消除行数越多得分越高
6. **等级递进**：根据消除行数提升游戏等级，等级越高方块自动下落速度越快
7. **游戏状态**：完整覆盖「待开始、游戏中、暂停、游戏结束」四种状态流转

### 3.2 界面与展示

1. **游戏主画布**：10×20 标准游戏网格，方块支持填充色 + 描边效果，落地前显示阴影预览
2. **信息面板**：展示下一方块预览、当前分数、当前等级、累计消除行数
3. **控制菜单**：包含开始游戏、暂停 / 继续、重新开始、难度选择功能入口
4. **结束弹窗**：游戏结束时展示最终得分、历史最高分与重开按钮

### 3.3 数据持久化

1. 本地存储历史最高分记录，刷新页面不丢失
2. 本地保存音效开关、难度偏好等用户配置

## 四、非功能需求

1. **性能要求**：游戏主循环稳定运行 60 帧，方块下落与消行动画无卡顿
2. **兼容性**：适配 Chrome、Edge、Firefox 等主流现代浏览器桌面端
3. **可维护性**：核心游戏逻辑与渲染代码分层隔离，模块职责清晰，便于后续功能扩展
4. **体积要求**：无重型引擎依赖，首屏加载体积精简，静态部署访问速度快